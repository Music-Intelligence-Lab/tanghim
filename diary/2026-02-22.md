# 2026-02-22 — Session 25

## Summary

Fixed octave centering formula — the slider bank viewport now correctly treats a maqam octave as 13 sliders (tonic through its octave above), not 12.

---

## Session 25: Octave Centering Fix (13 Notes)

### What Changed

**Centering formula corrected** — `centerMaqamOctave()` and the RangeScroller double-click handler both used `visibleCount - 11` to calculate viewport padding. This treated the octave as 12 slider positions (spanning 11 semitone gaps). In reality, a maqam octave includes the tonic's repeat at the top (e.g. C3 through C4 = MIDI 48–60), which is **13 sliders**. The formula now uses `visibleCount - 13`.

**Negative padding guard** — Added `Math.max(0, ...)` around the padding calculation. At minimum width (12 visible sliders), `Math.floor((12 - 13) / 2) = -1` would shift the viewport past the tonic. Clamping to 0 keeps the tonic flush left when the viewport can't fit the full 13-note octave.

### Code Changes

**App.tsx — `centerMaqamOctave()`**:
```typescript
// Before:
const padding = Math.floor((visibleCount - 11) / 2)

// After:
const padding = Math.max(0, Math.floor((visibleCount - 13) / 2))
```

**RangeScroller.tsx — double-click handler**: Same formula change.

**CLAUDE.md** — Updated curtain effect centering documentation.

### Behavior at Different Widths

| Visible sliders | Padding | Result |
|---|---|---|
| 12 (minimum) | 0 | Tonic flush left (can't fit full octave) |
| 13 | 0 | Exact octave: tonic to tonic+12, no extra |
| 14 | 0 | 1 extra slider on right only |
| 15 | 1 | 1 extra on each side, centered |
| 17 | 2 | 2 extra on each side, centered |

### Why 13, Not 12

A maqam spans one octave: from the tonic up to (and including) its repetition one octave above. In slider positions, that's 13 consecutive MIDI notes. The previous formula used 11 (treating 12 positions as spanning 11 gaps), which is an off-by-2 error when the goal is to center 13 physical sliders in the viewport.

---

## Session 26: Context-Aware Enharmonic IPN Labels via Maqam Detail API

### Problem

Slider IPN labels were hardcoded from a static `IPN_NAMES` array based on chromatic position. This meant the same pitch at chromatic index 6 always showed "F#" — but musically, Saba's 4th degree (ḥijāz) should show "Gb" (since F is already degree III), while Hijaz's 3rd degree should show "F#" (since G is degree IV). The API's maqam detail endpoint provides exactly this context-aware information.

### API Discovery

Verified the DiArMaqAr maqam detail endpoint returns context-aware `ipnReferenceNoteName` per degree:
- `GET /maqamat/maqam_saba?tuningSystem=angloeuropean_1800&startingNote=yegah&pitchClassDataType=all`
  - Degree IV (ḥijāz): `ipnReferenceNoteName: "Gb"`, `englishName: "Gb3"`
- `GET /maqamat/maqam_hijaz?...`
  - Degree III (ḥijāz): `ipnReferenceNoteName: "F#"`, `englishName: "F#3"`

Same pitch, different IPN based on maqam context. Also discovered the `availableTranspositions` field which maps `tonic.idName` → transposition `idName` for fetching transposition-specific detail.

### Architecture

**New struct — `MaqamDetailResult`** (`ApiResponseParser.h`):
```cpp
struct MaqamDetailResult {
    std::vector<PitchClass> ascendingDegrees;
    std::map<juce::String, juce::String> transpositionIdMap;  // tonicId → idName
};
```

**Data flow:**
1. User selects maqam → `applyMaqam()` runs instantly from maqam list cache (existing behavior, unchanged)
2. `applyMaqam()` calls `fetchAndApplyMaqamDetail()` at the end
3. `fetchAndApplyMaqamDetail()` checks maqam detail cache → if hit, applies IPN refs immediately; if miss, fetches from API
4. `applyDegreeIpnRefs()` populates `currentDegreeIpnRefs[12]` — one IPN string per chromatic slot
5. `notifyTuningChanged()` emits tuning state with new `degreeIpnMap`
6. NoteSliderBank uses `degreeIpnMap[chromaticIndex]` when available, falls back to `IPN_NAMES`

**Transposition handling:**
- Base maqam detail provides `availableTranspositions` → `currentTranspositionIdMap`
- Transposition detail is fetched with `&transpositionId=` parameter
- Each transposition is cached separately: key = `systemId:startingNote:maqamId:transpositionId`
- If the transposition map isn't available yet, fetches base maqam first, then re-invokes

### Code Changes

**C++ (6 files modified):**

- **`ApiResponseParser.h`** — Added `MaqamDetailResult` struct, changed `parseMaqamDetail()` return type
- **`ApiResponseParser.cpp`** — Updated `parseMaqamDetail()` to parse `pitchData.ascending` AND `availableTranspositions`
- **`DiArMaqArClient.h/.cpp`** — Changed `fetchMaqamDetail()` to use `pitchClassDataType=all`, added optional `transpositionId` parameter, changed callback type to `MaqamDetailResult`
- **`ApiDataCache.h/.cpp`** — Added maqam detail cache: `hasMaqamDetail()`, `getMaqamDetail()`, `storeMaqamDetail()`, disk persistence in `~/Library/Tanghim/cache/maqam-detail/`, JSON serialization/deserialization, lazy loading on startup scan
- **`PluginProcessor.h/.cpp`** — Added `currentDegreeIpnRefs[12]`, `currentTranspositionIdMap`, `getDegreeIpnRefs()` accessor, `fetchAndApplyMaqamDetail()` and `applyDegreeIpnRefs()` helpers. Called from `applyMaqam()`. Cleared on `loadTuningSystem()`.
- **`NativeBridge.cpp`** — Added `degreeIpnMap` object to `buildTuningStateJson()` — maps chromatic index → IPN string for populated slots

**TypeScript (3 files modified):**

- **`types/index.ts`** — Added `degreeIpnMap: Record<string, string>` to `TuningState`
- **`App.tsx`** — Added `degreeIpnMap: {}` to `EMPTY_STATE`, threaded `degreeIpnMap` prop to `NoteSliderBank`
- **`NoteSliderBank.tsx`** — Added `degreeIpnMap` prop, changed IPN label computation: `degreeIpnMap?.[String(chromaticIndex)] ?? IPN_NAMES[chromaticIndex]`

### Edge Cases Handled

- **No maqam selected**: `degreeIpnMap` is empty → all sliders use `IPN_NAMES` (unchanged behavior)
- **Detail not yet loaded**: Labels show default IPN until async fetch completes, then update via `tuningStateChanged` event
- **Tuning system switch**: `currentDegreeIpnRefs` cleared + `currentTranspositionIdMap` cleared
- **Transposition without base detail**: Fetches base maqam first to get `availableTranspositions` map, then re-invokes for transposition
- **User changes maqam during fetch**: Guard check `currentMaqamId != maqId` in callback
- **API error**: Labels stay as default IPN (degree highlighting still works from maqam list data)

### Solfege Consistency (Same Data Source)

After initial implementation, identified that solfege was still sourced from tuning system slot variants while IPN came from maqam detail. This meant IPN and solfege could be inconsistent (e.g., "Gb3" with "Fa#3"). Extended the feature to also source solfege from the maqam detail:
- Added `currentDegreeSolfegeRefs[12]` alongside `currentDegreeIpnRefs[12]`
- Added `degreeSolfegeMap` to tuning state JSON (parallel to `degreeIpnMap`)
- NoteSliderBank uses `degreeSolfegeMap[chromaticIndex]` when available, falls back to variant solfege
- API currently returns non-context-aware solfege (e.g. "Fa#3" for Gb), but the API author will fix this — plumbing is in place for when it does

### Key Design Decisions

1. **Same source for IPN and solfege** — Both come from the maqam detail API, ensuring consistency
2. **Async fetch, not blocking** — Maqam selection remains instant (from maqam list data). IPN/solfege labels update asynchronously when detail arrives
3. **Separate cache directory** — `~/Library/Tanghim/cache/maqam-detail/` keeps maqam detail separate from tuning system data
4. **No UI changes for non-degree sliders** — Only degree slots get context-aware IPN/solfege. Non-degree sliders keep tuning system defaults
5. **Reusing existing parseSinglePitchClass()** — Maqam detail ascending degrees use the same PitchClass parsing. Both `ipnReferenceNoteName` and `solfege` fields are already read

### Build Status

- C++ build: PASSES (universal binary, all formats)
- React UI build: PASSES
- All 3 C++ unit tests: PASS
- VST3 installed and signed

---

## Session 27: Status Bar Fixes (Clear Cache Button, Label Rename, MIDI Device Persistence)

### What Changed

**1. Clear Cache button added to native status bar**
- New `clearCacheButton` in `PluginEditor.h` (TextButton with "✕ Clear Cache" label)
- Positioned between MIDI drag button and Updates button (right-aligned, 80px wide)
- `onClick`: calls `processor.clearCache()` → `dataCache.clearAll()` (deletes `~/Library/Tanghim/cache/` and `~/Library/Tanghim/cache/maqam-detail/`) + emits status message "Cache cleared"
- `clearCache()` added as public method on `PluginProcessor`

**2. Label renamed**: `"Preset MIDI:"` → `"Preset MIDI Map Config:"` — width increased from 70px to 130px in `resized()` layout

**3. MIDI device persistence bug fixed**
- **Root cause**: `onMidiDeviceChanged()` in `PluginEditor.cpp` called `processor.setMidiPresetDevice()` but never called `saveSettingsToDisk()`. The device name was stored in memory and the device was opened, but the selection was lost on plugin restart
- **Fix**: Added `processor.saveSettingsToDisk()` after `setMidiPresetDevice()` in `onMidiDeviceChanged()`
- Channel persistence was already working (via `setMidiPresetChannel()` which calls `saveSettingsToDisk()` internally)
- `saveSettingsToDisk()` moved from private to public in `PluginProcessor.h` for editor access

### Code Changes

- **`PluginEditor.h`** — Added `clearCacheButton` member, renamed `midiPresetLabel` text
- **`PluginEditor.cpp`** — Button setup + styling in constructor, layout in `resized()`, `saveSettingsToDisk()` call in `onMidiDeviceChanged()`
- **`PluginProcessor.h`** — Moved `saveSettingsToDisk()` to public, added `clearCache()` declaration
- **`PluginProcessor.cpp`** — Added `clearCache()` implementation

### Build Status

- C++ build: PASSES
- All 3 C++ unit tests: PASS
- VST3 installed and signed

---

## Session 28: Reference Frequency Control

### What Was Built

A global concert pitch control ("Ref Freq") that shifts all 128 MTS-ESP frequencies by a cents offset. The control lives in the top-bar's right column, alongside the MTS-ESP status badges.

### Architecture

**Internal model**: `referenceCentsOffset` (double, default 0.0, range ±700 cents). Applied as a global frequency multiplier in `TuningEngine::updateTuning()` after building the per-slot frequency table:
```cpp
if (referenceCentsOffset != 0.0) {
    const double ratio = std::pow(2.0, referenceCentsOffset / 1200.0);
    for (auto& f : newFreqs) f *= ratio;
}
```

**Hz computation** (for display only):
```
defaultHz = 440 * 2^((referenceNoteMidi - 69) / 12)  // pure 12TET
currentHz = defaultHz * 2^(referenceCentsOffset / 1200)
```

`referenceNoteMidi` is the MIDI note of the tonic in octave 1 (from pitch class data with `pitchClassIndex==0, octave==1`). Changes when the starting note changes.

**APVTS parameter**: `ref_freq` (`AudioParameterFloat`, ±700.0, step 0.01) — exposed for DAW automation. Gesture marking via `beginRefFreqGesture()`/`endRefFreqGesture()` for proper automation recording.

**State persistence**: `referenceCentsOffset` saved/restored in `getStateInformation()`/`setStateInformation()`.

### C++ Changes

**TuningEngine.h/.cpp** — Added `referenceCentsOffset` parameter to `updateTuning()`. Applied as global multiplier after `buildFrequencyTable()`.

**PluginProcessor.h/.cpp**:
- New private members: `referenceCentsOffset`, `referenceNoteMidi`, `referenceNoteDisplayName`, `refFreqParam`
- New APVTS param `ref_freq` + listener registration
- Tonic detection in `doLoad()`: finds `pitchClassIndex==0, octave==1` in pitch class data
- Methods: `setReferenceCentsOffset()`, `finalizeReferenceCentsOffset()`, `getReferenceDefaultHz()`, `getReferenceCurrentHz()`, `beginRefFreqGesture()`, `endRefFreqGesture()`
- All `updateTuning()` call sites updated to pass `referenceCentsOffset` (9 sites via replace_all)
- `parameterChanged()` handler for `ref_freq` (DAW automation playback)
- State save/restore for `referenceCentsOffset`

**NativeBridge.cpp**:
- 4 JSON fields in `buildTuningStateJson()`: `referenceFreqCents`, `referenceFreqHz`, `referenceDefaultHz`, `referenceNoteName`
- 4 bridge functions: `setReferenceFreqCents` (fire-and-forget), `setReferenceFreqCentsFinalize` (returns full state), `beginRefFreqGesture`, `endRefFreqGesture`

### TypeScript Changes

**types/index.ts** — 4 new `TuningState` fields: `referenceFreqCents`, `referenceFreqHz`, `referenceDefaultHz`, `referenceNoteName`

**useJuceBridge.ts** — 4 bridge wrappers: `setReferenceFreqCents`, `setReferenceFreqCentsFinalize`, `beginRefFreqGesture`, `endRefFreqGesture`

### React Component — ReferenceFreqControl

**ReferenceFreqControl.tsx** (new file):
- SVG arc knob (36×36px, ±135° range mapped to ±700 cents)
- Drag: ~0.5 cents/px normal, ~0.1 cents/px with Shift (fine mode)
- Double-click: reset to 0
- Editable Hz input (type Hz value, converts to cents via `1200 * log2(hz / defaultHz)`)
- Read-only cents display (e.g. "+10.00 ¢")
- Semitone +/- buttons (±100 cents per click) flanking the cents display
- Gesture marking: mousedown → `beginRefFreqGesture()`, mouseup → `endRefFreqGesture()`
- RAF-throttled drag → `setReferenceFreqCents()` (fire-and-forget), mouseup → `setReferenceFreqCentsFinalize()`

**App.tsx** — Handlers: `handleRefFreqDrag` (optimistic local state + RAF bridge call), `handleRefFreqDragEnd` (finalize + state sync), gesture start/end wrappers.

### Follow-Up Fixes (Session 28b)

After initial implementation, user provided 4 corrections:

**1. Reverted CSS grid to flex layout**
- Top-bar was changed to CSS grid (2×2) in initial implementation. User preferred flex.
- Solution: `.top-bar` as flex row, added `.top-bar-left` (flex:1, column) and `.top-bar-right` (column) wrapper divs
- TuningSystemSelector.css: reverted from `display: contents` + grid placement to `flex-direction: column`
- ReferenceFreqControl.css: removed `grid-column`/`grid-row`
- OutputModeSelector.css: removed `grid-column`/`grid-row`

**2. Fixed reference freq not resetting on starting note change**
- User reported: changing starting note updated the Ref Freq dial/values but not the actual tuning
- Root cause: `referenceCentsOffset` was preserved across starting note changes (by design in original plan: "global concert pitch preference"). But user expected the offset to reset when the reference point changes
- Fix: reset `referenceCentsOffset` to 0 and sync APVTS `ref_freq` param in `loadTuningSystem()`, before `doLoad()`
- Now changing starting note always starts with a clean 0-cent offset

**3. Removed note name from ReferenceFreqControl**
- The component showed the PAO name of the reference note below the "Ref Freq" label
- User pointed out it's redundant with the starting note dropdown in TuningSystemSelector
- Removed `noteName` prop and its display section

**4. Added semitone +/- buttons**
- Two compact 20×20px buttons flanking the cents display: `-` and `+`
- Each click shifts by ±100 cents (one semitone), clamped to ±700
- Uses gesture-wrapped `onDragEnd()` for proper DAW automation recording

### Layout (Final)

```
.top-bar (flex row)
├── .top-bar-left (flex:1, column)
│   ├── Tuning System ▼
│   └── Starting Note ▼
└── .top-bar-right (column)
    ├── Ref Freq [knob] [98.00 Hz] [-][+10.00 ¢][+]
    └── MTS-ESP ⓝ  MPE ⓝ  Mono PB ⓝ
```

### Files Modified

| File | Change |
|------|--------|
| `source/engine/TuningEngine.h` | Added `referenceCentsOffset` param to `updateTuning()` |
| `source/engine/TuningEngine.cpp` | Apply offset as global frequency multiplier |
| `source/PluginProcessor.h` | Members, getters, setters, gesture methods, `refFreqParam` |
| `source/PluginProcessor.cpp` | APVTS param, doLoad() tonic detection, state save/restore, parameterChanged, all updateTuning() sites, reset on loadTuningSystem |
| `source/NativeBridge.cpp` | 4 JSON fields + 4 bridge functions |
| `ui/src/types/index.ts` | 4 new TuningState fields |
| `ui/src/hooks/useJuceBridge.ts` | 4 bridge wrappers |
| `ui/src/components/ReferenceFreqControl.tsx` | **New** — knob + Hz input + cents display + semitone buttons |
| `ui/src/components/ReferenceFreqControl.css` | **New** — styles |
| `ui/src/components/TuningSystemSelector.css` | Flex column layout |
| `ui/src/components/OutputModeSelector.css` | Removed grid placement |
| `ui/src/App.tsx` | Flex layout wrappers, ref freq handlers, wired component |
| `ui/src/App.css` | Flex top-bar with left/right columns |

### Build Status

- C++ build: PASSES (universal binary, all formats)
- TypeScript type check: PASSES
- All 3 C++ unit tests: PASS
- React UI build: PASSES
- VST3 installed and signed
