# 2026-02-19 — Session 14: Remove Output Mode from Transmitter

## Summary

Removed the output mode selector (MTS-ESP / MPE / Pitch Bend) from the Transmitter plugin. The Transmitter now exclusively broadcasts via MTS-ESP — output mode selection is the Receiver's responsibility. The UI selector was replaced with a read-only MTS-ESP connection status display.

---

## Architectural Decision

### Why Remove Output Mode?

The Transmitter had a 3-mode selector (MTS-ESP, MPE, Pitch Bend), but this was architecturally redundant:

1. **MTS-ESP** works directly — any MTS-ESP-aware synth reads tuning from shared memory. No Receiver needed.
2. **MPE / Mono PB** always require a Receiver plugin between the Transmitter and the synth.
3. Multiple Receivers can coexist on different tracks, each independently choosing MPE or Mono PB.

Having mode selection on both Transmitter and Receiver was confusing and unnecessary. The Transmitter's only job: **broadcast tuning via MTS-ESP**. The Receiver independently decides how to deliver it.

---

## C++ Changes

### TuningEngine (`source/engine/TuningEngine.h/.cpp`)
- Removed `OutputMode` enum entirely
- Removed `currentMode`, `mpeZoneConfigSent` fields
- Removed `setOutputMode()`, `getOutputMode()`, pitch bend range methods
- Removed `processMidi()` method (only did work in MPE/PB modes)
- Removed `mpe` and `monoPb` unique_ptr members
- Kept: `MtsEspTransmitter`, `updateTuning()`, frequency/cents tables, `isMtsTransmitter()`, `mtsNumReceivers()`

### PluginProcessor (`source/PluginProcessor.h/.cpp`)
- Removed `setOutputMode()`, `getOutputMode()`, `setMonoPitchBendRange()` methods
- Removed `outputMode` and `monoPitchBendRange` from state serialization/deserialization
- Removed `tuningEngine.processMidi()` calls from both `processBlock` overloads
- Old saved state with these properties loads without error (ValueTree ignores unknown properties)

### NativeBridge (`source/NativeBridge.h/.cpp`)
- Removed `setOutputMode` and `setMonoPitchBendRange` native function registrations
- Removed `outputMode` from tuning state JSON pushed to JS
- Removed `outputModeFromString()` and `outputModeToString()` helper methods
- Kept: `isMtsTransmitter` and `mtsReceivers` in state JSON for status display

**Note:** `MpePitchBendProcessor` and `MonoPitchBendProcessor` source files remain — they're still used by the Receiver plugin.

---

## React UI Changes

### OutputModeSelector → MtsEspStatus
Rewrote `OutputModeSelector.tsx` as a read-only status component:
- Shows "MTS-ESP" label + connection status
- Connected: "N receiver(s)" count
- Disconnected: "not connected" warning
- No interactive controls — purely informational

### Removed from codebase
- `outputMode` from `TuningState` type (`ui/src/types/index.ts`)
- `handleOutputModeChange` from `App.tsx`
- `setOutputMode` and `setMonoPitchBendRange` from `useJuceBridge.ts`

---

## Git Repository Setup

Created GitHub repo `KhyamAllami/tanghim` (private) and pushed the full project as initial commit. Description: "MIDI Tuning plugin for Arabic maqām via MTS-ESP, MPE or Mono Pitch Bend"

---

## M4L Fixes (from earlier in session)

## Plugin Rename: Arabic Maqam Tuner → Tanghim

### The Rename Journey

Renamed all user-facing plugin references from "Arabic Maqam Tuner" to "Tanghim" (the Arabic word for tuning/intonation). The process revealed several DAW/build constraints:

1. **First attempt**: `"Tanghīm (transmitter)"` — JUCE's CMake post-build `cmake -E remove` command doesn't quote paths, so parentheses `()` in PRODUCT_NAME cause a shell syntax error: `/bin/sh: -c: line 0: syntax error near unexpected token '('`

2. **Second attempt**: `"Tanghīm – transmitter"` (en-dash U+2013) — built successfully but Ableton's VST3 scanner garbled the UTF-8: displayed as "Tangh圮m 亍妤 transmitter" (CJK characters)

3. **Extended ASCII investigation**: ī (U+012B, Latin i with macron) is NOT in ISO 8859-1, Windows-1252, or any common "Extended ASCII" codepage. Macron vowels only exist in ISO 8859-4 (Baltic), which DAWs don't use.

4. **Final solution**: Pure ASCII `"Tanghim"` / `"Tanghim Receiver"` for all DAW-facing names (PRODUCT_NAME, `getName()`, scale name, Receiver editor title). UTF-8 `"Tanghīm"` kept only in the WebView UI `<title>`, which is rendered by WebKit and not constrained by DAW text handling.

### Files Changed
- `CMakeLists.txt` — PRODUCT_NAME, BUNDLE_ID, CLAP_ID
- `source/PluginProcessor.h` — `getName()` return value
- `source/PluginProcessor.cpp` — `buildScaleName()` default
- `source/PluginEditor.cpp` — Windows webview cache path
- `source/receiver/ReceiverEditor.cpp` — title label
- `source/api/ApiDataCache.cpp` — cache directory name
- `ui/index.html` — page title (UTF-8 "Tanghīm")
- `ui/package.json` — package name
- `m4l/generate_patch.py` — output paths, title, plug_vst3 message
- `m4l/mts_midi_effect.js` — comment

### Key Finding
**VST3 plugin names MUST be pure ASCII for Ableton compatibility.** The VST3 scanner (runs as x86_64 on Apple Silicon) cannot handle UTF-8 or any non-ASCII encoding in plugin bundle names or display names.

---

## M4L Fixes (from earlier in session)

### Max 9 Crash Fix
- Changed `plug "Arabic Maqam Tuner Receiver"` to `plug_vst3` in generate_patch.py
- Built Receiver in Release mode (Debug builds crashed Max 9's dyld loader)

### .amxd Binary Format
Reverse-engineered the Ableton `.amxd` container format:
- `ampf` header (4 bytes) + version 4 (LE u32) + `mmmmmeta` section + `ptch` section (JSON + null terminator)
- M4L metadata: `title`, `latency: 0`, `project.amxdtype: 1835887981` (0x6D696469 = "midi")
- Updated generate_patch.py to output both .maxpat and .amxd

### Mode Mapping Fix
Fixed inverted mode mapping in `mts_midi_effect.js`: live.menu index 0=MPE, 1=MonoPB now matches js code.

---

## Files Changed

### C++
| File | Changes |
|------|---------|
| `source/engine/TuningEngine.h` | Removed OutputMode enum, MPE/PB methods and members |
| `source/engine/TuningEngine.cpp` | Simplified to MTS-ESP only |
| `source/PluginProcessor.h` | Removed output mode methods |
| `source/PluginProcessor.cpp` | Removed mode state persistence and processMidi calls |
| `source/NativeBridge.h` | Removed mode-related declarations |
| `source/NativeBridge.cpp` | Removed mode native functions and helpers |

### React/TypeScript
| File | Changes |
|------|---------|
| `ui/src/components/OutputModeSelector.tsx` | Rewritten as MtsEspStatus (read-only) |
| `ui/src/components/OutputModeSelector.css` | Updated for status display styling |
| `ui/src/App.tsx` | Removed mode handler and outputMode from state |
| `ui/src/types/index.ts` | Removed outputMode from TuningState |
| `ui/src/hooks/useJuceBridge.ts` | Removed setOutputMode and setMonoPitchBendRange |

### Max for Live
| File | Changes |
|------|---------|
| `m4l/generate_patch.py` | plug_vst3 fix, .amxd binary output |
| `m4l/mts_midi_effect.js` | Mode mapping fix |
